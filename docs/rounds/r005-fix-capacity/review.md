---
title: r005 审查报告（阶段 3 用）
description: 验收对账、规则核对、文档对账、视觉对账与两栏处置清单（收官时定稿）。
type: reference
status: draft
owner: 陀梓皓
updated: 2026-09-19
---

<!-- overview -->
对照需求单 §5 的 E1~E8 逐条给证据；收官前为骨架。

## 1. 验收对账

| 条目 | 实现位置 | 证据 | 结论 |
| --- | --- | --- | --- |
| E1 列表卡满员态与数字 | 待实现 | | |
| E2 满员申请 409 + 系统消息 | 待实现 | | |
| E3 并发不变量 | 待实现 | | |
| E4 取票提速且行为不变 | 待实现 | | |
| E5 六类系统消息 | 待实现 | | |
| E6 在场标记不受影响 | 待实现 | | |
| E7 前端文案统一 | 待实现 | | |
| E8 门禁三项 | 待实现 | | |

## 2. 规则核对（AGENTS.md）

| 规则 | 核对方式 | 结论 |
| --- | --- | --- |
| 未批不实现；每处改动挂 `rNNN` | 提交信息 `[Req: r005]`；批复原文在需求单 §3 | 待核 |
| 一次提交一个逻辑增量；`git add` 具体路径 | `git log` | 待核 |
| 零新依赖、不改端口 | 依赖文件 diff + `dev.bat` diff | 待核 |
| 密钥不入库 | `git grep` | 待核 |
| 文档与代码同提交 | 逐提交 diff | 待核 |
| 行为变更记 ADR + 模块页变更记录 | ADR-0016 + `r001-rooms*.md` 追加行 | 待核 |

## 3. 文档对账

| 项 | 结论 |
| --- | --- |
| 模块轴（设计页 / 实现页 / 功能页 / 教学页） | 待核 |
| 轮次轴（design/changes/review + ADR） | 待核 |
| 索引 + roadmap + 覆盖矩阵无 planned 残留 | 待核 |

## 4. 视觉对账（界面类）

| 组 | 方式 | 结果 |
| --- | --- | --- |
| 文案口径 | 真机截图（列表卡 / 状态条 / 系统消息） | 待测 |
| 满员态徽标 | 真机截图 | 待测 |
| 零 emoji / 单一图标库 | 扫描 | 待测 |

## 5. 留给用户的两栏处置清单

**已落地可保留的增量**：待实现后填。
**未闭合项 / 半成品**：待实现后填（预期候选：演示库现存超容量房间的处理、是否给举手/焦点也加系统消息）。

## 6. 合并指引（由人执行）

```bash
git checkout main
git merge --no-ff req/r005-fix-capacity
git tag -a round-r005-done -m "r005 完成（容量按在册 + 房间事件系统消息 + 取票提速）"
```

合并后复验：`pytest backend/tests -q` → `python backend/scripts/smoke.py` → `cd frontend && npx tsc --noEmit && npm run build`。
