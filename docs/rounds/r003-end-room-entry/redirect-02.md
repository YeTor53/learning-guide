---
redirect: r003-02
status: confirmed-C
raised_at: bb685ee（2026-09-19）
decided_by: 用户 2026-09-19「构思这种结束后房间的管理，然后加入归档」；六条分支点批复「2 1（但是数据截至至他离开）1 2 1 1」（2026-09-19）
---
# redirect r003-02：构思「结束后房间的管理」并加入归档

## 1. 用户原话 / 现象

> 「构思这种结束后房间的管理，然后加入归档」（2026-09-19）

（承接上下文：刚验证完「房主结束房间」入口；你接着要的是**结束之后**这条线怎么设计。两个读法都覆盖：① 把构思写成文档归档；② 产品上做一个「归档」面 —— 本单按 ① 出页，页内 §4/§12 A1~A3 已把 ② 设计成可拍板的形态。）

## 2. 比对依据（三比）

- **本轮（r003）已批准需求单 / 设计 / 验收**：`docs/00-requirements/r003-end-room-entry.md` §2「不做」明确「不做『结束后房主留在只读页』新视图（M4 纪要页再谈）」，§1 目的只覆盖「结束入口」→ 判定：**本轮范围没有这条**。
- **roadmap / backlog**：`docs/00-project/global-roadmap.md` §9 台账有「`ended` 房间的回看载体（历史 + 纪要）」并写「归 M4，M4 之前不做」；`docs/99-archive/r001-ahead-m4-summaries.md` 有纪要设计但**展示位置写的是已被删除的详情页** → 判定：**M4 里程碑下有，本轮无**。
- **当前产物实况**（实测 2026-09-19）：`ended` 房间在列表页只有「已结束」徽标、卡片无动作（`article` 内按钮数 = 0）；库侧数据齐全（`rooms.ended_at`、`room_members.exit_reason`、`join_requests` 全历史、`chat_messages`）→ 能力缺口是**入口与视图**，不是数据。

## 3. 定性

**C 后续工作**（本轮范围外，roadmap 已排到 M4）：按 2.2 登记进 `docs/99-archive/`（`status: backlog`）+ roadmap §9 回填；**本轮 r003 照常收官**，不夹带实现。

## 4. 需求单口径

- **本轮 r003 需求单不改**（范围仍是「r002 收官回填 + 房主结束房间入口」）。
- 产出物 = 构思页 `docs/99-archive/r003-ahead-m4-ended-rooms-archive.md`（`status: backlog`）：① 术语与状态（归档 = `ended` 的只读视图，**不是新状态**）② 权限矩阵（**曾参与即可读**，动契约 L3）③ 承载形态三选项对比（推荐 B 归档总览 + A 单房间归档页）④ 页面信息架构（A 六块 / B 筛选与行 / 列表页卡片改「查看归档」+ 侧边栏入口）⑤ 接口与函数级草案（`list_archived_rooms` / `was_member` / `get_archive_detail`）⑥ **零迁移结论**（配额数据全在，只缺「谁移出了谁」等 M5 项）⑦ 与 M4 纪要设计的接口（**修正**：纪要展示位置从已删除的详情页改到归档页）⑧ 验收点草案 ⑨ 成本与归属（建议与 M4 纪要同轮）⑩ FQ-A1~A6。

## 5. 冲突项清单

| 冲突点 | 依据 | 处置 | 影响的文档与代码 |
| --- | --- | --- | --- |
| 与 r003 §2「不做只读新视图」冲突？ | `r003-end-room-entry.md` §2 | **不冲突**：本条判 C，不进 r003 实现；r003 范围一字不改 | 无 |
| 与 M4 纪要设计的展示位置冲突 | `r001-ahead-m4-summaries.md`「房间详情页可查看」 | **登记为 M4 开轮必改**：改为归档页纪要区（构思页 §8 已写修正） | 该 backlog 页（M4 开轮时改） |
| 与「房间 = 一场讨论」终态口径冲突？ | ADR-0012 条 2 | **不冲突**：归档只读、不可重开、写入动作全无 | 构思页 §1/§3 |
| 与「列表页 = 发现新讨论」定位冲突 | 功能页页面口径 | 已处置：推荐把归档放进 `/archive`，列表页 `ended` 卡片只留一个「查看归档」动作（§5.3） | 功能页（M4 开轮时改） |

## 6. 复用清单

| 已落地增量 | 位置 | 新结论下是否仍有效 |
| --- | --- | --- |
| 一次性讨论终态与连带动作（`ended` / 成员 `room_ended` / 申请 `cancelled`） | `services/rooms.py:end_room` | 有效，归档直接读这些数据 |
| 只读消息渲染与成员行 | `r001` 详情页能力（代码已随页删除，设计在 `r001-rooms.md`） | 设计可复用，M4 重写组件 |
| `redirect-06` 的删除决定 | `docs/rounds/r002-livekit/redirect-06.md` | 有效：归档是**在别处**补回访，不回退删除决定 |

## 7. 处置

- **C**：登记 `docs/99-archive/r003-ahead-m4-ended-rooms-archive.md`（`status: backlog`）+ roadmap §9 该行回填指向本页；M4 开轮时移回 `docs/02-modules/` 并改前缀。
- 本轮 r003 继续：等你审 `docs/rounds/r003-end-room-entry/review.md` → 你合并并打 `round-r003-done`。

## 8. 结论（用户一次批复）

- 批复原文（第一次）：「构思这种结束后房间的管理，然后加入归档」（2026-09-19）——按上表落地为构思页 + backlog 登记。
- 批复原文（第二次，六条分支点）：「**2 1（但是数据截至至他离开）1 2 1 1**」（2026-09-19）→ 依次对应 A1~A6：
  | 编号 | 批复 | 含义 |
  | --- | --- | --- |
  | A1 | 2 | 归档入口**只列表页**（不做侧边栏入口） |
  | A2 | 1 | 可见范围 = **曾参与**，**补充口径：数据截至到他离开** → 构思页新增 §3.1「离开即截止」与服务端截断 |
  | A3 | 1 | 承载形态 = **B 归档总览 + A 单房间归档页** |
  | A4 | 2 | 房主**可软删归档 + 保留期**（加 `rooms.archived_hidden_at` 迁移 + 恢复接口） |
  | A5 | 1 | **不显示**「谁移出了谁」（不加 `exit_by`） |
  | A6 | 1 | **M4 不做导出** |
- 批复原文（第三次，三条追加）：「**1 1 3（但是设计时，给出软删的数据将不被保护，会在必要时被清理）**」（2026-09-19）→ 依次对应 A7~A9：
  | 编号 | 批复 | 含义 |
  | --- | --- | --- |
  | A7 | 1 | B 归档总览**只从列表页「已结束」视图进**，侧边栏不动 |
  | A8 | 1 | 已离开者**不给看纪要**（含他离开后内容） |
  | A9 | 3 | **不设保留期**：软删即永久隐藏、可随时恢复；并要求**文档写明「软删的数据不再受保护，必要时会被清理」**（写入构思页 §6 数据保护口径 + 界面二次确认文案） |
- 至此 A1~A9 全部拍板；构思页 §12 已把九条决定与三处新增（§3.1 截断、软删/恢复含 `ARCHIVE_PURGED`、不受保护口径）写死，M4 开轮直接照搬。
- 落地提交 SHA：见 `changes.md` 台账（r003 附加 docs 增量）。
