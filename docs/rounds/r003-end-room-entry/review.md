---
title: r003 审查报告（阶段 3 用）
description: r003 的验收对账、规则核对、两轴文档对账、CR 与重定向对账、视觉对账、两栏处置清单与合并指引（收官时定稿）。
type: reference
status: draft
owner: 陀梓皓
updated: 2026-09-19
---

<!-- overview -->
阶段 3 的产物：对照需求单逐条给证据、对照 `AGENTS.md` 与 `docs/04-style/` 核对规则、两轴文档对账、CR 与重定向对账、视觉对账；
并给用户两栏处置清单（可保留增量 / 未闭合项）。**收官前本页为骨架。**

## 1. 验收对账（需求单 §4 逐条）

| 验收条目 | 实现位置 | 证据 | 结论 |
| --- | --- | --- | --- |
| V1 | 待实现后填 | | |
| V2 | | | |
| V3 | | | |
| V4 | | | |
| V5 | | | |
| V6 | | | |
| V7 | | | |
| V8 | | | |
| V9 | | | |
| V10 | | | |
| V11 | | | |

## 2. 规则核对（AGENTS.md / 风格指南）

| 规则 | 核对方式 | 结论 |
| --- | --- | --- |
| 未批不实现；每处代码改动挂在 rNNN | 提交信息 `[Req: r003]` | 待核 |
| 一次提交一个逻辑增量；`git add` 具体路径；append-only | `git log --oneline` | 待核 |
| 密钥不入库（`API_SECRET` 等） | `git grep` 检索 | 待核 |
| 文档与代码同一次提交 | 逐提交 diff 比对 | 待核 |
| 零 emoji / 单一图标库 Lucide | `frontend/src` 扫描 | 待核 |

## 3. 文档对账（两轴 + 四件套 + 覆盖矩阵）

| 项 | 结论 |
| --- | --- |
| 模块轴：设计页 / 模块页 / 使用者教学页 / 开发者教学页 | 待核 |
| 轮次轴：`docs/rounds/r003-end-room-entry/` 三件套齐 | 待核 |
| 索引：`docs/00-requirements/README.md` 有 r003 行；模块页变更记录已回填 | 待核 |
| 覆盖矩阵：无空格、无 `planned` 残留 | 待核 |

## 4. 重定向与 CR 对账

| 单号 | 类型 | 级别 | 结论 | 落地 SHA |
| --- | --- | --- | --- | --- |
| r002-07 | 重定向（实现偏差 + 入口位置变更） | L3 | confirmed-A，并入 r003 | 见 changes.md |
| （实现期追加） | | | | |

## 5. 视觉对账（界面类项目）

| 组 | 方式 | 结果 |
| --- | --- | --- |
| 令牌扫描 | `git grep -- --live-end` | 待测 |
| 零 emoji / 单一图标库 | 扫描 `frontend/src` | 待测 |
| 桌面 + 窄屏截图 | 真机（你的 `5173` + `8000`） | 待测 |
| 几何量测 | 控制坞按钮尺寸/位置与「离开」一致性 | 待测 |

## 6. 留给用户的两栏处置清单

**本轮已落地可保留的增量**（各带 SHA 与文档页）：待实现后填。

**未闭合项 / 半成品**（各带「若判为 C 或 D 时的处置」）：待实现后填（预期候选：`redirect-03` 原生弹窗替换仍 proposed；`redirect-02` 的 G2/G3 待批；M4 纪要页与 `ended` 回看载体）。

## 7. 合并指引（由人执行，AGENTS 硬规矩 4）

```bash
git checkout main
git merge --no-ff req/r003-end-room-entry
git tag -a round-r003-done -m "r003 完成（房主结束房间入口 + r002 收官回填）"
```

合并后复验：`npx tsc --noEmit` → `npm run build` → `pytest backend/tests -q`（95 passed）→ 真机 V1~V5。
