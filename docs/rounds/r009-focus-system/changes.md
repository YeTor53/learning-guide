---
title: r009 台账：焦点系统重做
description: cp 切分、每步实测数字与门禁记录。
type: reference
status: draft
owner: 陀梓皓
updated: 2026-09-20
---

<!-- overview -->
需求见 `docs/00-requirements/r009-focus-system.md`；设计见 `design.md`。

| cp | 内容 | 状态 | 提交 | 证据 |
| --- | --- | --- | --- | --- |
| cp-r009-0 | 阶段 1 文档（需求单 + 函数级 design + ADR-0021 + 台账/审查骨架） | **完成 2026-09-19** | `1c4af8e` | E12 |
| cp-r009-1a | 几何纯函数（均分铺满 + 加权焦点）+ 校验脚本（不动旧实现，门禁保持绿） | **完成 2026-09-19** | 见 cp-1a 提交 | E1/E2 |
| cp-r009-1b | 舞台组件切到新几何 + FLIP 动效 + 令牌 + 真机量测 | **完成 2026-09-19**（两个「没铺满」真问题已修） | `2819bd7` / `e3571be` | E0/E1/E2 |
| cp-r009-2a | 焦点权限**后端**（迁移 008 + 申请/批准/拒绝 + 用例） | **完成 2026-09-19** | 见 cp-2a 提交 | E7/E8 |
| cp-r009-2b | 焦点权限前端（角色分流按钮 / 申请批准面板 / 退出焦点） | **完成（协管分支待补验）** | 见 cp-2b 提交 | E6/E9/E10 |
| cp-r009-3a | 举手闪烁 + 格上「给焦点/放下手」角标 + 后端 `grant_focus_from_hand` | **完成 2026-09-19（闭环已验）** | 见 cp-3a/cp-3b 提交 | E3/E4 通过；E5 待补验 |
| cp-r009-4 | 声波（设备条已有 `.live-level` 电平，未新增控件） | **未完成（无独立提交）** | —（无） | E11 部分：声波复用 r002 已落地的 `.live-level`（引入提交 `04b6903`），本轮只接线；**数值序列未取** → 顺延 r011（见 `review.md` 未闭合清单 ①） |
| cp-r009-5 | 收官（门禁 + 实现页/功能页 + review 定稿） | **完成 2026-09-19** | 见 cp-5 提交 | E12 § |

### cp-1a 实测（`node frontend/scripts/verify-stage-geometry.mjs`，区域 1160×660 = 1440×900 下的实际舞台）

| 场景 | 结果 |
| --- | --- |
| 均分 1 人 | 面积差 0.0%｜铺满率 **100.0%**｜右缘 1160/1160 |
| 均分 2 人 | 0.0%｜**98.5%** |
| 均分 3 人 | 0.5%｜**97.0%**（改前 73.2% —— 空槽重罚修好） |
| 均分 4 人 | 0.0%｜**97.6%** |
| 均分 6 人 | 0.5%｜**96.1%** |
| 均分 8 人 | 0.6%｜**94.6%**（改前 84.7%） |
| 焦点 3/4/6/8 人 | 面积份量 **1.40×**（3 人单列：高一维 1.4×；4/6/8 人：宽 1.4×）｜无重叠｜不溢出 |
| 共享 3/6 人 | mode=share，共享格 1160×507（76.8% 高），人像铺满底部条 |

**两处实现比草案改得更好的地方**（都写回 design.md）：焦点从「跨 2 格」改「加权行列」（1.40× 而非 2× 横条）；`bestGrid` 空槽惩罚 0.35 → 2.0。

### cp-1b 真机量测与两个「没铺满」真问题（2026-09-19，你在屏幕上看到后报的）

**你报的现象**：舞台上只有三条矮格子，下面一大片空白（截图 `%TEMP%\lg_r009\stage-3p.png`）。

根因两层，都已修：

| # | 根因 | 修法 |
| --- | --- | --- |
| 1 | 我给 `.live-stage-grid` 加的 `height: 100%` 把 flex 撑高覆盖成 0（父级高度 auto）→ 舞台可用区恒为 0 | 去掉 `height: 100%`（`.live-stage` 的 `flex: 1` 负责拿高度）→ 实测可用区 **1368×631** |
| 2 | 旧的 `.live-tile:not(.live-tile-focus) { flex: 0 0 168px }`（r004 缩格条定宽）+ 无高度 → **格子铺满了，格子里的画面块只有 131px 高** | 在 `.live-stage-grid` 作用域内覆盖：`.live-cell > .live-tile { width/height: 100% }`、`.live-tile-focus` 取消 aspect-ratio 限制、非焦点格不再压暗；画面填充方式做成令牌 **`--tile-fit`（默认 `cover`=铺满可能裁边；改 `contain`=完整画面留黑边）** |

修后实测（1440×900，3 人）：

| 项 | 数字 |
| --- | --- |
| 舞台可用区 | 1368 × 631 |
| 铺满率 | **98.5%**（2 人 99.3%） |
| 面积差 | 0.2%（2 人 0.0%） |
| **cell vs tile** | 450×631 vs **450×631**（完全撑满，`tileFill=true`） |
| 截图 | `stage-3p-filled.png` |

另外两条如实记录：

1. ~~动效暂未验到~~ → **已验到（同日晚补测）**：关键是 ① 无头 Chromium 默认 `prefers-reduced-motion: reduce`，必须先 `page.emulate_media(reduced_motion='no-preference')`；② 采样器要**事件驱动**（变化后再采 40 帧），固定 60/90 帧会在第三人进房前就跑完。实测（第三人进入瞬间，1440×900）：

| 帧 | 格数 | 动画数 | 动画时长 | 尺寸（逐帧收敛） |
| --- | --- | --- | --- | --- |
| t=2391ms | 2 | 0/0 | — | 679×631 \| 679×631 |
| **t=2429ms（变化瞬间）** | 3 | **1/1/1** | **240/240/220** | 679×631 \| 679×631 \| 413×581 |
| t=2455ms | 3 | 1/1/1 | 240/240/220 | 613×631 \| 612×631 \| 424×596 |
| … | … | … | … | 逐帧收敛 |
| t=2647ms | 3 | 1/1/1 | 240/240/220 | **450×631 \| 449×631 \| 449×631** |

- 时长 **240ms = `--stage-move-ms`**、**220ms = `--stage-enter-ms`**（新格进入）→ 令牌真的在生效；
- 单帧最大位移约 **66px**（679→613），远低于「格宽 60% = 270px」的防瞬移阈值 → 是连续过渡，不是一步跳到位；
- 收敛到 450/449/449 与纯函数理论值一致。

### cp-1b 追加（你的要求）：本地用户格子标识

- 你说「加个（）你标识」→ 本地用户那格右上角加一枚 **「（你）」** 胶囊（`.live-cell-self`，`pointer-events: none`，仅本地格出现），实测该类元素数 = 1；截图 `stage-self-marker.png`。
2. C 离开后 1.2 秒内仍显示 3 格：LiveKit 的断线判定本身有数秒延迟（不是布局 bug），离场格延迟摘除要在那个时点才谈得上。

### cp-2a（焦点权限后端 E7/E8）

- 迁移 `008_r009_focus_requests.sql`：`focus_requests`（pending/approved/rejected/cancelled + 待批唯一索引 `ux_focus_requests_pending`），`db_init` 应用后 `schema_migrations=8`。
- `services/focus_requests.py`：`request_focus`（协管申请，可在批→幂等；房主调用 → 400「请直接取得焦点」）、`list_focus_requests`（Host/Moderator）、`decide_focus_request`（**本人批准 → 403 `SELF_APPROVAL`**；批准同事务设焦点 + 清申请人举手）、`cancel_pending`。
- 路由：`POST/GET /api/rooms/{id}/focus-requests`、`POST /api/focus-requests/{id}/approve|reject`；错误码新增 `SELF_APPROVAL`。
- 用例 4 条（真实库、回滚事务）：协管申请 + 幂等 + **本人批准 403**；房主批准 → 焦点落到申请人且**举手被清**；拒绝 → 焦点不变；房主申请 400、参与者申请 403。
- 门禁：`pytest` **128 passed**（原 124）。
- 实现期踩到的两个真坑（都已在代码里写明原因）：① `assert_room_role` 返回的是 **MemberRow** 而不是角色字符串（`role == "host"` 永远不成立 → 房主申请被误当协管通过）；② `room_hand_raises.lowered_reason` 的 CHECK 只允许 `self/other/room_ended`，给焦点时放手的理由必须用 **`other`**（我用 `focus_granted` 直接撞约束、500）。另：新增迁移后 `test_schema` 的期望列表要跟着加（否则门禁红）。

### cp-2b / cp-3（前端）：真机实测（2026-09-19，三人三浏览器）

| # | 断言 | 实测 |
| --- | --- | --- |
| 1 | 举手键按角色分流 | **房主 = 「取得焦点」** ✓、参与者 = 「举手」 ✓；**协管分支未验到**（下方说明） |
| 2 | 房主一键取得焦点 | 点一下 → 文案变「**退出焦点**」、焦点格数 = 1；**协管端同时看到焦点格 = 1**（两端一致） |
| 3 | 退出焦点 | 再点一下 → 文案回「取得焦点」、焦点格 = 0 |
| 4 | 举手格持续闪烁（cp-3） | 参与者举手 → 房主端 `.live-cell.is-hand` = **1**，动画 **1200ms**、`animation-name: hand-breathe`（= `--hand-blink-ms`） |
| 5 | 待批焦点申请面板 | 房间抽屉「成员」tab 内置待批区块（`.focus-request-row`）＋批准/拒绝；**本人申请只显示「需另一位管理身份批准」不显示批准按钮**（后端 403 `SELF_APPROVAL` 已在用例覆盖） |

**未验到的一项（如实）**：协管的「申请焦点」分支没跑通 —— 我的验证脚本用 `POST /api/rooms/{id}/members/{uid}/role` 给协管赋权，**该端点不是正确的赋权入口**（脚本没断言返回值就往下走了），所以第三人在页面上仍是参与者（按钮显示「举手」）。产品侧的协管逻辑（申请 → 他人批准 → 得焦点）已由后端 4 条用例覆盖（含本人批准 403），**前端协管按钮分支待下次真机补验**。

**本轮尚未做**：格子上的「给焦点 / 放下手」角标按钮（E4/E5 的 UI 部分；后端 `grant_focus_from_hand` 也还没写）。这是我下一件的头等事。

### cp-3a：举手 → 管理「给焦点 / 放下手」（E4/E5）

- 后端：`services/focus.grant_focus_from_hand`（一个事务里设焦点 + 把该人举手放下，`lowered_reason='other'`）+ 路由 `POST /api/rooms/{room_id}/focus/from-hand`。
- 前端：举手格上出现「**给焦点 / 放下手**」角标（仅管理身份、且该人未处于焦点时）；`RoomLivePage.grantFocusFromHand` 调接口后刷新焦点与举手。
- 真机（三人三浏览器）实测：

| # | 断言 | 结果 |
| --- | --- | --- |
| 1 | 三角色按钮文案（修正后） | **房主=「取得焦点」、协管=「申请焦点」、参与者=「举手」** ✓（上一轮协管分支没验到，是因为我赋权用错了方法——正确入口是 **`PATCH /rooms/{id}/members/{uid}/role`**，不是 POST） |
| 2 | 举手格上的角标 | 房主端 `.live-cell.is-hand`=1、角标按钮=**2**（给焦点 / 放下手）✓ |
| 3 | 点「给焦点」 | **未通过**：焦点格 0、举手未清、参与者端文案仍「放下手」→ 说明前端那次 POST **静默失败**（我的 try/catch 把错误吞了，下一轮先加网络日志定位） |
| 4 | 协管申请焦点 | **未通过**：点「申请焦点」后文案没变「已申请焦点」、房主端待批行 0 → 同样是前端 POST 静默失败（后端 4 条用例已证明接口本身正确） |

- 下一步（下一轮开工第一件）：给这两处加上响应日志/错误提示，定位是 `request()` 的调用形状、还是 404（路由未注册到 `/api`）——**最可疑的是新路由的注册**（`room_extras` 的 router 前缀与 `focus/from-hand` 路径需复核）。

### cp-3b：定位并修掉「前端 POST 静默失败」

**根因（不是代码错，是我自己的环境错）**：后端进程是我为了绕开 Windows 上 uvicorn `--reload` 自崩而**用「无 reload」方式起的**，而 `/focus-requests`、`/focus/from-hand` 是那之后才加的路由 → 旧进程返回 **404**，又被我前端 `try/catch` 吞掉，屏幕上什么都没发生。重启后端后两条路由立刻可用（未登录返回 **401** 而不是 404，可自证路由已注册）。

顺手修掉三处**真实**的同步问题（新接口本身没错，缺的是「让全场知道」）：

1. 「给焦点」不再直接打新接口，改为走既有的 `focus.setFocus` + `hands.lowerOther`（这两条带 DataChannel 广播），新接口作为**原子兜底**在后台 fire-and-forget；
2. 批准焦点申请后，由**批准人**的客户端补一次焦点广播（否则申请人自己看不到焦点变化）；
3. 协管点「申请焦点」后**立即刷新**自己的待批状态（按钮文案马上变「已申请焦点」，不用等 8 秒轮询）。

**真机复验（三人三浏览器，1440×900）**：

| 断言 | 实测 |
| --- | --- |
| 举手 → 管理点「给焦点」 | 房主端 **焦点格=1、举手格=0**（举手被清）；**参与者端焦点格=1 且按钮变「退出焦点」** → 两端同步 ✓ |
| 举手格角标 | 2 个按钮（给焦点 / 放下手）✓ |
| 「放下手」路径 | 本轮未验到（测试脚本用 API 直接清焦点、没有广播，参与者端仍以为自己是焦点，于是点按钮变成「退出焦点」而不是举手）——**脚本问题，非产品缺陷**；下一轮用真机点「退出焦点」再复验 |
| 协管申请 | 上一轮已验证：房主端待批行=1 ✓；文案即时更新与批准后的两端同步待补验 |

## 收官门禁（cp-5）

| 项 | 结果 |
| --- | --- |
| `pytest backend/tests` | **128 passed** |
| `smoke --base-url http://127.0.0.1:8000` | **PASS 46/46** |
| `tsc --noEmit` / `npm run build` | exit 0 / built |

## 焦点闭环真机复验（三人三浏览器，1440×900）

| # | 场景 | 实测 |
| --- | --- | --- |
| 1 | 举手 → 房主点「给焦点」 | 房主端 焦点格=1、举手格=0；参与者端 焦点格=1、按钮变「退出焦点」（两端同步） |
| 2 | 房主点「放下手」 | 举手格 1 → **0**；参与者端按钮回到「举手」 |
| 3 | 协管点「申请焦点」 | 文案即时变「**已申请焦点**」；房主端成员面板待批行 = 1 |
| 4 | 房主点「批准」 | **三端焦点格均为 1**（房主/协管/参与者），协管文案「退出焦点」 |
| 5 | 协管点「退出焦点」 | 房主端焦点格 0、协管文案回「申请焦点」 |
| 6 | 麦克风电平控件 | 未静音时 `.live-level` 元素存在；**数值序列未取到**（我采的是容器宽度恒 13px，真正的电平量在内层 fill / CSS 变量）→ E11 部分完成 |

## r009.5 补正（2026-09-20，轮次 `req/r009.5-debt-backfill`）

- **补 tag**（只新增、不改历史）：`cp-r009-0` → `1c4af8e`、`cp-r009-1b` → `e3571be`、`cp-r009-3a` → `0f438a2`；`cp-r009-4` **不打**（无独立提交，见上表）。
- **两处结论冲突的裁定**：本文 §cp-3b 曾写「『放下手』路径本轮未验到」，同文件末尾「焦点闭环真机复验」表 #2 写「举手格 1 → 0」。**裁定以末尾复验表为准**：cp-3b 当时那条未验到是**脚本问题**（脚本用 API 直接清焦点、没有广播，参与者端仍以为自己是焦点，于是点按钮变成「退出焦点」），非产品缺陷；`cp-r009-3b` 之后已用真机点「退出焦点」复验通过。原文保留备查。
- 其余补正（review 定稿、轮次号纠错、索引回填、死代码、取证）见 `docs/00-requirements/r009.5-debt-backfill.md` 与 `docs/rounds/r009.5-debt-backfill/`。
