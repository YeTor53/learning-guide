---
title: r008 审查报告（阶段 3）
description: E1~E12 逐条证据、规则/文档/视觉对账、两栏清单与合并指引（cp-5 定稿）。
type: reference
status: draft
owner: 陀梓皓
updated: 2026-09-20
---

<!-- overview -->
状态：**已定稿**（cp-5 = 收官提交 `b52f91f`）。**r009.5 补正（2026-09-20）**：去掉「骨架」误标、E5 补「未取证」标注、§5 未闭合项轮次号改 r010、§6 合并顺序补 r005 起点（本轮无行为变更）。

## 1. 验收对账

| 条目 | 证据 | 结论 |
| --- | --- | --- |
| E1 迁移 007 落库 | `db_init` 应用 `007_r008_session_summaries`，表计数出现 `session_summaries` | **通过** |
| E2 生成纪要（成功/失败/未配置三分支） | 成功分支**真机**：房主在已结束房间生成 → **6.0 秒 / 632 字 / 五节齐全 / 不编造**（`ready` 落库）；失败分支与未配置分支由用例覆盖（502 落 `failed` 行、503 不落库） | **通过** |
| E3 重复生成覆盖同行 | 用例：第二次生成后 `id` 不变、库里仍 1 行、内容更新；真机按钮显示「重新生成」 | **通过** |
| E4 查看权限与空值 | 用例：成员 200 / 非成员 403 / 未生成 `summary: null`；真机未生成时页面显示「还没有纪要」 | **通过** |
| E5 前端纪要页与入口 | 已结束卡片按钮「讨论纪要」→ `/rooms/{id}/summary` → 房主可见「生成讨论纪要」→ 点击生成 → 正文渲染 → 按钮变「重新生成」；截图 `summary-ready.png` | **通过**（**子项「未配密钥给出明确提示」：未取证**——`changes.md §3.2` 自述当时 `LLM_API_KEY` 已填故未复现（探针 2.5 秒 vs 真实耗时 4.6~6.8 秒，请求仍在飞）；本条如实标注，不因代码写了就当已过） |
| E6 冒烟补「生成纪要」步 | `smoke` **PASS 46/46**，含「生成讨论纪要 → ready（实测 559 字）」与「查看纪要 → 200」；未配密钥时走 503 分支的断言也在脚本里（如实两分支） | **通过** |
| E7 生成邀请（参数与校验） | 用例：非管理身份 403；`ttlSeconds=61` → 400 `VALIDATION`；30 秒 → 201（码 6 位小写、`maxUses=1`、`usedCount=0`、`expired=false`） | **通过** |
| E8 用码加入（有效/过期/用尽/满员/幂等） | 用例：有效 → 201 + 角色 `participant` + 系统消息「通过邀请链接加入」+ `used_count=1`；同一人再来 → 200 幂等且不重复计数；乱码/用尽/过期 → 400 `INVITE_INVALID`；**满员 → 409 `ROOM_FULL`**（实测抓到「已用完把老成员挡门外」的顺序缺陷并修正） | **通过** |
| E9 前端邀请入口与凭码加入页 | 抽屉「邀请」tab（仅管理身份）→ 生成 30 秒码 → 行内倒计时「29 秒后过期 · 已用 0/1」→ 客人 `/join?code=` 预填 → 加入直接进房、成员数 1→2、系统消息「客人乙 通过邀请链接加入」 | **通过** |
| ~~E10 转写上传~~ | 你 2026-09-19 决定：转写整体移至 **r010**（默认开启 + 并入文字对话 + 含管理信息 + 先出 STT 获取方案）→ 本轮不适用 | **移出本轮** |
| ~~E11 转写开关与片段列表~~ | 同上，移至 **r010**（r009.5 复验时抓到本行漏改，已修） | **移出本轮** |
| E12 门禁与文档同步 | `pytest` 124 / `smoke` 46/46 / `tsc` / `build` 全绿；README（数字 + 9 步演示路径）、`.env.example`（`INVITE_TTL_MAX_SECONDS`）、实现页/功能页/ADR-0018/0019、作业对照页均已同步 | **通过** |

## 2. 规则核对（AGENTS.md）

| 规则 | 核对 | 结论 |
| --- | --- | --- |
| 未批不实现；改动挂 `rNNN` | 需求单 §2 有你 2026-09-19 的批复；提交信息带 `[Req: r008]` | **通过** |
| 一次提交一个逻辑增量 | cp-0 / cp-1 / cp-2(+2b) / cp-3a / cp-3b / cp-5 共 7 个提交 | **通过** |
| 密钥不入库 | 新增只读 `LLM_*`（你本机 `.env`，未入库；`.env.example` 仅键名） | **通过** |
| 外部调用唯一出口 + 失败不回滚业务 | `services/summary.call_llm` 是唯一 LLM 出口；失败落 `failed` 行另记原因（ADR-0018） | **通过** |
| 文档与代码同提交 | 每个 cp 都带文档 | **通过** |
| 不改历史迁移文件 | 新增 `007_…`，未动 001~006 | **通过** |

## 3. 文档对账

| 项 | 结论 |
| --- | --- |
| 模块轴 | 实现页 `02-modules/r008-assignment-gaps.md`、功能页 `…-features.md`、ADR-0018/0019 | **齐** |
| 轮次轴 | `rounds/r008-assignment-gaps/{design,changes,review}.md` | **齐**（review 定稿） |
| 索引 | 需求索引、模块 README、docs/README、roadmap、作业对照页 | **齐** |
| 交付物 | README（含 9 步演示路径）、`.env.example`、冒烟说明；**「使用的 AI 工具与模型列表」仍缺**（见 §5） | **部分** |

## 4. 视觉对账

| 组 | 方式 | 结果 |
| --- | --- | --- |
| 纪要页 | 截图 `summary-empty.png` / `summary-ready.png`（整页） | 暗底卡片、正文 1.85 行高、按钮态清晰 |
| 邀请面板 | 截图 `invite-panel.png` | 表单三件套（有效期/次数/生成）+ 行内码与倒计时；失效行置灰 0.55 |
| 凭码加入页 | 截图 `join-page.png` | 与纪要页同一壳（`.summary-shell`），码输入等宽字体 |
| 零 emoji / 单一图标库 | 扫描 | 仅 Lucide（`FileText/Sparkles/AlertCircle/Link2/Copy/Timer/KeyRound`） |
| 动效降级 | 既有 reduced-motion 块覆盖 | 新面板无新动效（只有倒计时文本） |

## 5. 两栏处置清单

**已落地可保留**

1. 纪要链路（表 + 服务 + 路由 + 页面 + 用例 + 真机 6.0 秒/632 字/反编造）。
2. 限时邀请（6 位码、最长 1 分钟、凭码直接进、容量仍挡、幂等；真机两窗口验证）。
3. 顺手修：`RoomVO.myRoleAny`（结束后权限判断）、撤回接口此前 403 的根因、README 数字漂移、冒烟补 6 步。

**未闭合项 / 如实说明**

| # | 项 | 状态 |
| --- | --- | --- |
| 1 | 「使用的 AI 工具与模型列表」（作业交付物） | **缺**：需你填（用了哪些工具/模型）；我可以先起一页模板 |
| 2 | 语音转文字（含 STT 获取方案） | 已整体移到 **r010**（原定 r009，2026-09-19 焦点系统插入后顺延；你的口径：默认开启 + 并入文字对话 + 含管理信息） |
| 3 | 结束后自动生成纪要 | 未做（只做一键）；要自动需加后台任务 |
| 4 | 纪要版本历史 / 长邀请码 / 服务端混音录制 | 均未做（ADR 写明取舍） |
| 5 | 加分项：Docker Compose、管理后台、演示录屏 | 未做 |

## 6. 合并指引（由人执行）

```bash
git checkout main
git merge --no-ff req/r008-assignment-gaps   # 建议顺序：r005 → r006 → r007 → r008；其后 r009 → r010 → r009.5
git tag -a round-r008-done -m "r008 完成（讨论纪要 + 限时邀请）"
```

合并后复验：`pytest backend/tests -q` → `python backend/scripts/smoke.py` → `cd frontend && npx tsc --noEmit && npm run build`。
