---
title: Learning Guide 学习讨论室 — 项目规划（roadmap）
description: 本项目唯一的项目级规划：起点现状、终局目的、里程碑与待拍板项。
type: concept
status: draft
owner: 陀梓皓
updated: 2026-09-16
---

<!-- overview -->
面向深度学习主题的多人音视频讨论室（作业题目 A）的**项目级规划**。本文件只定方向与验收点，不写实现细节（细节归 `docs/01-architecture/`、`docs/02-modules/`）；里程碑只定方向，不承诺日期。

## 1. 起点现状（2026-09-16 实测）

- **项目性质**：全新项目；作业两题中选**题目 A：Learning Guide 学习讨论室**。
- **交付形态**：可演示的迷你产品网站（完整前端 + 服务端 + SQL 设计 + 设计说明）；通过后会被追问实现细节、边界情况与取舍。
- **时间与人力**：单人；作业要求收到后 4 个自然日内提交，约 16 小时专注工作量。
- **本机环境实测**：`node v22.19.0`、`npm 10.9.3`、`git 2.51.0` 可用；**无 docker、无 PostgreSQL 客户端、无 LiveKit 凭据**。
- **可复用资产**：无。题面明确不得直接使用 LiveKit Meet 默认页面，须基于 LiveKit SDK / Components 自行实现。
- **外部归宿与口径**：产物交给 First Light Tech 评审。
  - 压缩包命名口径：`AI管培生_<姓名>_题目A或B_<日期>.zip`；提交时注明所选题目与实际投入小时数。
  - 必须声明所用 AI 工具与模型；使用开源模板须注明来源并说明自己新增与修改的部分。
  - 源码可用 Git 仓库链接（口径见 P8）。

## 2. 终局目的

一句话：**做一个能现场演示的学习主题讨论室** —— 用两个浏览器完整走通「注册 → 建房 → 等候室审批 → 音视频 / 屏幕共享 / 举手 / 焦点发言 / 群聊 → 服务端踢人 → 结束房间 → LLM 生成讨论纪要并在房间详情查看」，且全部数据落库、关键链路可用脚本复现。

成功标准（可观测，逐条可验证）：

1. 双浏览器演示路径全通：8 人上限拒绝并有明确提示、等候室审批、Host / Moderator / Participant 三种角色权限可复现。
2. LiveKit Token 由服务端签发；仓库与前端产物中检索不到 Secret（给出检索命令与结果）。
3. SQL schema + 初始化脚本一条命令可跑，至少含 `users`、`rooms`、`room_members`、`join_requests`、`chat_messages`、`session_summaries`，表间关系在设计说明中有说明。
4. 冒烟脚本一条命令跑通「创建用户 → 建房 → 签发 Token → 写聊天 → 生成纪要」并打印结果。
5. 设计说明覆盖：架构、权限矩阵、表结构、LiveKit 与两个自定义能力、等候室与踢人流程、纪要生成链路、失败与边界情况、安全、未完成项。

非目标（明确不做）：多租户与计费、移动端 App、生产级运维（K8s / 监控告警）、自研音视频传输、UI 设计体系（用现成组件库起量）。

## 3. 里程碑

| 里程碑 | 内容 | 验收点（可演示/可验证） | 依赖 | 预估轮次 |
| --- | --- | --- | --- | --- |
| M1 骨架 · 账户 · 房间 | 前后端骨架、数据层 schema + 种子、注册/登录/登出、房间创建（主题/标题/简介）/列表/详情、加入申请的数据结构 | 未登录不可建房/加入；注册登录可用；一条命令初始化并写入演示账号与示例房间；房间列表/详情显示真实库数据 | — | 2~3 |
| M2 实时房间 · 权限 · 等候室 | LiveKit 接入（服务端签 Token）、等候室申请→批准/拒绝→进入、三角色权限矩阵、8 人上限、参与者列表、离开、结束房间、服务端踢人 | 双浏览器：A 建房 → B 申请 → A 批准 → B 进入；第 9 人加入被拒并提示；Host 踢出成员后该成员被强制断开（服务端 API 触发，非前端假踢） | M1、LiveKit 凭据（P3） | 3~4 |
| M3 自定义能力 · 群聊 | 群聊实时收发 + 落库 + 按房间拉最近消息；举手（实时同步、列表展示、Moderator/Host 可放下他人举手）；焦点发言（放大/取消恢复）；屏幕共享布局切换；焦点与共享并存的优先级规则 | 每条能力在两浏览器可见且一致；刷新/重进后消息与状态与库一致；优先级规则在文档中写明且演示与规则一致 | M2 | 3~4 |
| M4 会后产出 · 交付 | LLM 纪要生成链路（输入含聊天记录 + 参与者 + 主题，落库，房间详情可查看）、冒烟脚本、设计说明 + README + `.env.example`、加分项按余力、打包提交 | 冒烟脚本全绿并贴输出；设计说明各章齐全；提交物命名符合口径、AI 工具清单齐全 | M1~M3、LLM 凭据（P4） | 2~3 |

加分项（非必须，按余力排在 M4 之后）：断线重连后举手/焦点状态恢复；录制或旁路录音转写后再生成纪要；Docker Compose 一键启动；简单管理后台；3–5 分钟演示录屏。

## 4. 待拍板项（批准 roadmap 前逐条定）

| 编号 | 事项 | 选项 | 建议值 | 影响 |
| --- | --- | --- | --- | --- |
| P1 | 题目确认 | 题目 A（LiveKit 讨论室）/ 题目 B（候选池与适合度） | 题目 A（下列内容按 A 写） | 若为 B，本规划整份重写 |
| P2 | 工作区位置 | 现建位置 `G:\VSCODE\VS_items\LearningGuide-LiveKit` / 换到 `F:\working\` 下 / 自定路径 | 现位置 | 仅影响路径，可整体搬迁 |
| P3 | LiveKit 来源 | LiveKit Cloud 免费项目（注册即得 URL + Key/Secret）/ 自建（需先装 Docker，本机暂无） | Cloud 免费项目 | 阻塞 M2 验收；自建要多花环境时间 |
| P4 | 纪要 LLM | 复用本机已有的 DeepSeek Key（OpenAI 兼容）/ 另配 OpenAI、Anthropic、OpenRouter | DeepSeek（`deepseek-chat`） | 影响 LLM 调用模块与 `.env` 字段 |
| P5 | 数据库 | SQLite（零安装，文件库）/ PostgreSQL（需装服务） | SQLite（SQL 语法保持可迁移） | 影响 schema 写法与启动步骤；题面二者皆可 |
| P6 | 技术栈 | Next.js（App Router，前后端一体）/ React(Vite) + Node(Express) 前后端分离 / Python 后端 | Next.js（一体仓，提交与演示最简单） | 决定 M1 骨架形态，批准后不宜再换 |
| P7 | 加分项取舍 | 断线重连状态恢复 / Compose / 管理后台 / 录屏 / 全不做 | 只做「断线重连状态恢复」，其余在 M4 视余力 | 影响 M4 范围与 16 小时预算 |
| P8 | 提交方式 | GitHub 仓库链接（公开/私有）+ zip / 仅 zip | 公开仓库 + zip（便于评审复现） | 影响是否要建远端与 README 对外可读性 |

## 5. 风险与假设

- **凭据阻塞**：无 LiveKit 凭据则 M2 无法验收（最大风险，P3 优先定）。
- **复杂度集中**：屏幕共享 / 焦点发言 / 二者的优先级规则是三处最容易出坑的实时状态，需在 M3 先定规则再实现。
- **时间预算**：16 小时对「必做全项」偏紧，加分项一律后置；若超时，优先保证 M1~M4 必做闭环可演示。
- **LLM 纪要**：依赖外部 API 的可用性与限流，须设计降级（生成失败时可见错误 + 可重试，不阻塞房间结束）。
- **安全**：Secret 泄露是硬扣分项，`.env` 不入库 + 前端产物检索作为固定验证步骤。

## 6. What's next

1. 用户逐条拍板 P1~P8（尤其 P1、P3、P6）。
2. 本文件 `status: draft → approved`。
3. 开 `r001` 需求单（映射里程碑 M1），再出设计页 → 批准后进入实现。
