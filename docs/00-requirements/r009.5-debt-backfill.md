---
title: r009.5 需求单：r008/r009 欠账补正（台账 · tag · 审查回填 · 纠错 · 取证）
description: 两轮收官一致性体检查出的欠账逐条修复——补 cp tag、回填台账与审查结论、纠正轮次号与路径、删零引用死代码、补真机证据与教学页。只修欠账，不加功能。
type: reference
status: approved
owner: 陀梓皓
updated: 2026-09-20
---

<!-- overview -->
来源：你 2026-09-20 让「把前面的 r008 r009 检查一遍」→ 体检查出 22 条欠账（见本页 §6 出处列）→ 你批复「**用一轮.5修复，按你想的来**」（授权我定做法与范围）。
分支 `req/r009.5-debt-backfill`，自 `req/r010-transcription` HEAD（`941046f`）开出；**合并顺序排在 r010 之后**：r005 → r006 → r007 → r008 → r009 → r010 → r009.5（r010 的阶段 1 文档已在本分支的祖先里，先合 r010 再合本分支不会夹带）。
设计见 `docs/rounds/r009.5-debt-backfill/design.md`（逐文件改动清单）。

## 1. 需求（R）

| # | 需求 | 出处 | 现状 |
| --- | --- | --- | --- |
| R1 | **补 cp tag**：`cp-r009-0`、`cp-r009-1b`、`cp-r009-3a`、`cp-r010-0` 打到既有提交上；`cp-r009-4` 不打（无独立提交，如实写进台账） | AGENTS.md 硬规矩 4（实现期打 cp tag） | r009 只有 1a/2a/2b/3b/5；r010 一个都没有（提交信息却自称 cp-0） |
| R2 | **回填 r009 台账**：cp 表里 cp-0「进行中」、cp-1b「进行中」、cp-4「部分」与实际不符（提交都在） | r009 `changes.md` §1 | 三行状态未回填 |
| R3 | **r009 审查报告定稿**：E0~E12 逐条结论（E0 现在**缺行**）、门禁数字、合并顺序、未闭合清单 | r009 `review.md` | E1/E2/E12 仍写「待测 planned」，而数字早就在 `changes.md` 里 |
| R4 | **消 r009 文档自相矛盾两处**：① cp-3b 段写「放下手路径未验到」vs 同文件末尾复验表 #2 写「1 → 0」② review overview「cp-5 定稿时补齐」vs cp-5 已标完成 | 同上 | 同一文件两处结论冲突 |
| R5 | **r008 审查报告纠错**：overview「骨架（cp-5 定稿）」自相矛盾；E5 的「未配密钥提示」子项如实标未取证；§5 未闭合项轮次号 r009→r010；§6 合并顺序补 r005 起点 | r008 `review.md` | 四处表述不实/不全 |
| R6 | **轮次号与路径纠错**：9 处把转写轮次写成 r009（实为 r010），其中 3 处指向**不存在**的 `docs/rounds/r009-transcription/redirect-01.md` | r008 需求单 4 处 / design 4 处 / review 2 处 / `ai-tools-and-models.md` 1 处 | 死链 + 轮次号错 |
| R7 | **旧实现文件名纠错**：文档里 2 处仍写 `stageLayout.ts` / `computeStageLayout`（实为 `stageGeometry.ts` / `computeStageGeometry`） | r009 需求单 cp-1 行、r009 `motion-design.md` overview | 文档与代码不一致 |
| R8 | **配置面补齐**：`.env.example` 增 `STT_BASE_URL` / `STT_API_KEY` / `STT_MODEL` 三键（空值 + 注释）——r008 需求单 E12 与 r010 需求单「`.env` 已有三键预留」都指向它，实测两处都没有 | r008 需求单 §3-E12、§4；r010 需求单 §3 | 承诺未落地 |
| R9 | **清零引用死代码**（L2，你已授权）：删 `frontend/src/components/live/stageLayout.ts`（ADR-0021 已取代、全仓 0 引用）；`services/repositories` 的 `cancel_pending*` **保留**并标明「已备未接」（r011 接事件时用） | r009 review 未闭合项 ③；本轮实测 `git grep` | 1 个死文件 + 2 个死函数（后者登记不删） |
| R10 | **索引与台账回填**：需求索引缺 r010 行、r004 行仍写「待合并」（实际已合并 `daf7696` + `round-r004-done`）、顺序乱；`docs/README.md` §2「本轮（r004）」过时、§4 缺 r009/r010 行、§6 实测清点写 61 个 .md（实测 122）；roadmap §3 M3 无状态回填、§7 只写到 r005、§9 有一处去向写错（转写写 r011）与一行截断 | 本轮体检 | 索引层漂移 8 处 |
| R11 | **真机取证补跑**：① E0 动效（终点误差、过渡时长、无瞬移）② E11 声波（元素与 CSS 链路 + 截图，数值序列按纪律如实标未取证）③ 落盘 r009 实现页点名的 `mic-wave.png` | r009 需求单 E0/E11；r009 实现页 §截图行 | E0 无结论、E11 部分、点名文件不存在 |
| R12 | **补两页教学页**：使用者页（纪要 / 限时邀请 / 焦点系统，实跑为准）+ 开发者页（舞台几何与动效量测脚本用法）——r008/r009 的 C/D 件套欠账 | 铁律 6（每轮四件套）；r008/r009 的 `rounds/` 档案里都没有教学页 | 两轮缺 C/D |

## 2. 口径与边界

- **只修不改**：产品行为、接口路径与字段、数据模型、迁移文件、依赖、端口一律不动。
- **tag 只新增**：指向既有提交；不改历史、不 amend、不 rebase、不删既有 tag。
- `.env` 不动（钥匙只在你手里）；只改 `.env.example` 的键名与注释。
- **不合并、不打 `round-*` tag**：合并与轮次 tag 由你执行（AGENTS.md 硬规矩 4）。
- 本轮**不做**（明确出界）：r010 转写实现；`cancel_pending` 接事件（r011）；E5「未配密钥提示」真机（需临时改 `.env`，如实标未取证）；r006/r007 的教学页（登记 `docs/README.md` §6 与 roadmap §9，不在本轮补）。

## 3. 验收（E）

| # | 条目 | 证据 |
| --- | --- | --- |
| E1 | `git tag --list 'cp-r009*' 'cp-r010*'` 含 0/1a/1b/2a/2b/3a/3b/5 + `cp-r010-0`，且各自指向台账所列提交 | 命令输出 |
| E2 | r009 `changes.md` cp 表无「进行中」残留；cp-4 行写明「无独立提交 + 原因」 | 文件 |
| E3 | r009 `review.md` 无「planned / 待测」残留，E0~E12 每行有数字或截图路径，含合并顺序 | 文件 |
| E4 | r008 `review.md` overview 无矛盾；E5 行含「未取证」标注；§5 轮次号 = r010；§6 顺序含 r005 | 文件 |
| E5 | 死链 0 命中（**本轮文档描述该错误的引用除外**）：`git grep -n "r009-transcription" -- docs | grep -v "r009.5"` 为空；`git grep -n "移至 r009" -- docs` 为空 | 命令输出 |
| E6 | `.env.example` 含三键 `STT_*`（键名 + 注释），无真值 | 文件 |
| E7 | `git grep -n stageLayout -- frontend/src` = 0 命中；`tsc`/`build` 仍绿 | 命令输出 |
| E8 | 需求索引含 r009/r010/r009.5 行且 r004 行标已合并；`docs/README.md` §2/§4/§6 与 roadmap §3/§7/§9 已回填 | 文件 |
| E9 | E0 动效真机：终点误差 ≤1px、过渡时长 = `--stage-move-ms` 实测值、无瞬移采样；交替说话 30 秒 ≤3 次（未取证则如实标注） | 脚本输出 + 采样序列 |
| E10 | E11 声波：`.live-level` 内层 CSS 链路证据 + `mic-wave.png` 落盘；数值序列如实标注 | 截图 + DOM 断言 |
| E11 | 门禁：`pytest` / `smoke` / `tsc` / `build` 全绿 | 命令输出 |
| E12 | 两页教学页落地，使用者页步骤在真机复跑过（纪要 + 邀请 + 焦点） | 截图 + 实跑记录 |

## 4. 影响面

- 文档：22 处（`docs/` 下 11 个文件 + 5 个模块页「变更记录」各 1 行）。
- 代码：删 1 个文件（`stageLayout.ts`），无行为变更；无迁移、无接口变更。
- 配置：`.env.example` +3 键。
- 数据：不碰库。
- 新增产物：2 个取证/教学页 + 1 个量测脚本（`frontend/scripts/verify-stage-motion.py`，不新增仓库依赖——用本机 conda base 已装的 Playwright）。

## 5. 风险与假设

| 风险 | 处置 |
| --- | --- |
| 补 tag 指向旧提交被看作「回改已收官内容」 | 只读操作、可 `git tag -d` 撤销；r009/r010 均**未合并**，cp tag 属实现期产物，补号合法 |
| 删 `stageLayout.ts` 有隐藏引用 | 删除后必须复跑 `tsc --noEmit` + `npm run build`（E7） |
| review 回填的数字是「搬」不是「造」 | 每个数字标出处（`changes.md` 节或提交 SHA）；本轮新测的数字单独标「r009.5 实测」 |
| E11 声波真值取不到 | 按既有纪律不发布假麦；如实标「元素与链路已证、数值未取」，顺延 r011（登记 review 未闭合清单） |
| `.env` 未配 `STT_*` 时 r010 功能不可用 | 本轮只补键名占位；真机转写取证仍等你的 key（r010 需你的事项不变） |

## 6. 文档产出清单（覆盖矩阵）

| 件套 | 页 | 路径 | 状态 |
| --- | --- | --- | --- |
| 需求单 | 本页 | `docs/00-requirements/r009.5-debt-backfill.md` | landed |
| A 设计页 | 逐文件改动清单 | `docs/rounds/r009.5-debt-backfill/design.md` | landed |
| B 实现同步页 | 轮次台账 | `docs/rounds/r009.5-debt-backfill/changes.md` | landed |
| B 实现同步页 | 审查报告 | `docs/rounds/r009.5-debt-backfill/review.md` | landed |
| C 使用者教学页 | 纪要 / 限时邀请 / 焦点系统走一遍 | `docs/tutorials/r009.5-r008-r009-user-guide.md` | **landed**（cp-5，全流程真机复跑） |
| D 开发者教学页 | 舞台几何与动效量测脚本 | `docs/tutorials/r009.5-stage-motion-verify-dev.md` | **landed**（cp-5，含实测数字与三条踩坑） |
| 模块轴 | r005~r009 各页「变更记录」追加补正一行（这 5 页原本**没有**该小节，按 r001/r002/r004 既有先例补齐） | `docs/02-modules/r005-fix-capacity.md`、`r006-ui-sync-polish.md`、`r007-topic-and-scrollhint.md`、`r008-assignment-gaps.md`、`r009-focus-system.md` | **landed**（cp-5） |
| 索引轴 | 需求索引 + `docs/README.md`（§2/§4/§5/§6 重扫）+ roadmap（§3/§7/§9）+ `docs/tutorials/README.md` | 四处 | **landed**（cp-3 + cp-5） |
| ADR | **不适用**：无口径/契约变更；唯一代码改动（删零引用文件）为 L2 内部结构，记在 design.md「本轮设计变更记录」 | — | 不适用（理由如上） |

## 7. cp 切分

| cp | 内容 | 对应 |
| --- | --- | --- |
| cp-1 | 阶段 1 文档：需求单 + design + changes/review 骨架 + 索引登记行 | E3/E11 |
| cp-2 | 台账与 tag 补正：r009 changes 回填 + 4 个 cp tag + r008/r009 review 修正回填 | E1~E4 |
| cp-3 | 文档纠错与索引回填：轮次号/路径/旧文件名/合并顺序 + `docs/README.md` §6 重扫 + roadmap §3/§7/§9 | E5/E8 |
| cp-4 | 配置与死代码：`.env.example` 三键 + 删 `stageLayout.ts` + 注释同步 | E6/E7 |
| cp-5 | 门禁与取证收官：pytest/smoke/tsc/build + E0 实测 + E11 截图 + 两页教学页 + review 定稿 + 模块页变更记录 + 索引最终回填 | E9~E12 |

## 8. 变更记录

| 日期 | 版本 | 改了什么 | 依据 |
| --- | --- | --- | --- |
| 2026-09-20 | v1 | 建页：R1~R12 + 边界 + E1~E12 + 覆盖矩阵 + cp 切分 | 你 2026-09-20「用一轮.5修复，按你想的来」+ 本轮体检 22 条 |
| 2026-09-20 | v2 | **收官回填**：覆盖矩阵全部转 `landed`；新增两项**超出原计划**的发现与处置 —— ① 死链实修从 3 处扩到 **8 处**（另查出 `r006-fix-mic-badge` ×2、r004 教学页名 ×3）② 新登记「**三人档焦点同步观测**」（复现 2/2、未归因、本轮不修，见 r009 `review.md` 未闭合 ⑤）；E0 第 4 项与 E11 数值如实标**未取证** | 本轮 cp-5 实测；`.env.example` 三键仍等你填值 |
